import * as React from "react";
import * as ReactDOM from "react-dom";
import App from './App';    /* Main app file to be rendered on DOM */

ReactDOM.render(
    <App />,
    document.getElementById("mainRoot")
)